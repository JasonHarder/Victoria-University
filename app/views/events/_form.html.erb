
<%= form_with model: @event && @picture, local: true do |form| %>

  <% if @event.errors.any? || @picture.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@event.errors.count, "error" || @picture.errors.count,"error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @event.errors && @picture.errors.full_messages.each do |msg|%>
          <li> <%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <% if user_signed_in? %>
        <%= form.label :title%><br>
    <% end %>
        <%= form.text_field :title %>
  </p>

  <% if user_signed_in? %>
    <%=form.label :picture  %><br>
  <% end %>
    <%= form.text_area :picture %>

  <p>
    <% if user_signed_in? %>
        <%= form.label :url %> <br>
    <% end %>
        <%= form.text_field :url %>
  </p>

  <p>
    <% if user_signed_in? %>
        <%= form.label :body %><br>
    <% end %>
        <%= form.text_area :body %>
  </p>


  <p>
  <% if user_signed_in? %>
    <button class = "submit"> <%= form.submit %> </button>
  <% end %>
  </p>

<% end %>